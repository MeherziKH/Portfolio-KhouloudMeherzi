{% extends 'base.html.twig' %}
{% block title %}Ajout{% endblock %}

{% block body %}
    <div class="lonon-about">
        List des catégories
        {% for post in postsList %}
            <p> - {{ post.title }}</p>
        {% endfor %}
    </div>
{% endblock %}