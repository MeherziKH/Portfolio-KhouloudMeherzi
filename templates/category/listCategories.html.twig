{% extends 'base.html.twig' %}
{% block title %}Ajout{% endblock %}

{% block body %}
    <div class="lonon-about">
       List des catégories
        {% for category in categoriesList %}
            <p> - {{ category.title }}</p>
        {% endfor %}
    </div>
{% endblock %}